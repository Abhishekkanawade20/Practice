- name: local playbook
  hosts: all
  tasks: abcv
      - name: vuhj
        debug: 
          msg: "{{URL}}" 
  vars: 
   URL: "www.google.com" 
   URLt: "one"
  tasks: 
      - name: 
        debug: 
          msg: "{{URLt}}"
        
           
