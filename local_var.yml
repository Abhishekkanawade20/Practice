- name: local playbook
  hosts: all
  tasks: 
      - name: 
        debug: 
          msg: "{{URL}}" 
  vars: 
   URL: "www.google.com" 
   URLt: "one"
  taskst: 
      - name: 
        debug: 
          msg: "{{URLt}}"
        
           
