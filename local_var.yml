- name: local playbook
  hosts: all
   tasks: 
      - name: 
        debug: 
          msg: "{{URL}}" 
  vars: 
   URL: "www.google.com" 
  tasks: 
      - name: 
        debug: 
          msg: "{{URL}}}"
        
           